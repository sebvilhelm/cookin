/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MyDinnersComponent } from './my-dinners.component';

describe('MyDinnersComponent', () => {
  let component: MyDinnersComponent;
  let fixture: ComponentFixture<MyDinnersComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MyDinnersComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MyDinnersComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
