/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DinnerFormComponent } from './dinner-form.component';

describe('DinnerFormComponent', () => {
  let component: DinnerFormComponent;
  let fixture: ComponentFixture<DinnerFormComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DinnerFormComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DinnerFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
