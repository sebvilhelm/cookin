/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AdminDinnersListComponent } from './admin-dinners-list.component';

describe('AdminDinnersListComponent', () => {
  let component: AdminDinnersListComponent;
  let fixture: ComponentFixture<AdminDinnersListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AdminDinnersListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AdminDinnersListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
