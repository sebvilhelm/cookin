/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EditDinnerComponent } from './edit-dinner.component';

describe('EditDinnerComponent', () => {
  let component: EditDinnerComponent;
  let fixture: ComponentFixture<EditDinnerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EditDinnerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EditDinnerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
